version: '2'
services:
  # duwamish flavor:
  ${FLAVOR_1_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /$FLAVOR_1
    environment:
      - FLAVOR=${FLAVOR_1}
      - SITE_KEY=${FLAVOR_1_KEY}
      - SITE_URL=${FLAVOR_1_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_1_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_1_PORT}:${FLAVOR_1_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_1_PORT}
      - FLAVOR=${FLAVOR_1}
      - SITE_KEY=${FLAVOR_1_KEY}
      - SITE_URL=${FLAVOR_1_URL}
      # - CLICKY_ANALYTICS_ID=${DUWAMISH_CLICKY_ANALYTICS_ID}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  # duwamish dev flavor:
  ${FLAVOR_2_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /$FLAVOR_2
    environment:
      - FLAVOR=${FLAVOR_2}
      - SITE_KEY=${FLAVOR_2_KEY}
      - SITE_URL=${FLAVOR_2_URL}
    command: sh -c "git fetch && git checkout develop && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_2_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_2_PORT}:${FLAVOR_2_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_2_PORT}
      - FLAVOR=${FLAVOR_2}
      - SITE_KEY=${FLAVOR_2_KEY}
      - SITE_URL=${FLAVOR_2_URL}
      # - CLICKY_ANALYTICS_ID=${DUWAMISH_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout develop && git pull --rebase && /platform/start.sh"

  # trees flavor:
  ${FLAVOR_3_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /${FLAVOR_3}
    environment:
      - FLAVOR=${FLAVOR_3}
      - SITE_KEY=${FLAVOR_3_KEY}
      - SITE_URL=${FLAVOR_3_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_3_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_3_PORT}:${FLAVOR_3_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_3_PORT}
      - FLAVOR=${FLAVOR_3}
      - SITE_KEY=${FLAVOR_3_KEY}
      - SITE_URL=${FLAVOR_3_URL}
      # - CLICKY_ANALYTICS_ID=${FLAVOR_3_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  # snoqualmie flavor:
  ${FLAVOR_4_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /$FLAVOR_4
    environment:
      - FLAVOR=${FLAVOR_4}
      - SITE_KEY=${FLAVOR_4_KEY}
      - SITE_URL=${FLAVOR_4_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_4_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_4_PORT}:${FLAVOR_4_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_4_PORT}
      - FLAVOR=${FLAVOR_4}
      - SITE_KEY=${FLAVOR_4_KEY}
      - SITE_URL=${FLAVOR_4_URL}
      # - CLICKY_ANALYTICS_ID=${DUWAMISH_CLICKY_ANALYTICS_ID}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  # willamette flavor:
  ${FLAVOR_5_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /$FLAVOR_5
    environment:
      - FLAVOR=${FLAVOR_5}
      - SITE_KEY=${FLAVOR_5_KEY}
      - SITE_URL=${FLAVOR_5_URL}
    command: sh -c "git fetch && git checkout lukeswart/updates && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_5_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_5_PORT}:${FLAVOR_5_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_5_PORT}
      - FLAVOR=${FLAVOR_5}
      - SITE_KEY=${FLAVOR_5_KEY}
      - SITE_URL=${FLAVOR_5_URL}
      # - CLICKY_ANALYTICS_ID=${DUWAMISH_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout lukeswart/updates && git pull --rebase && /platform/start.sh"

  # lakewashington flavor:
  ${FLAVOR_6_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /${FLAVOR_6}
    environment:
      - FLAVOR=${FLAVOR_6}
      - SITE_KEY=${FLAVOR_6_KEY}
      - SITE_URL=${FLAVOR_6_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_6_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_6_PORT}:${FLAVOR_6_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_6_PORT}
      - FLAVOR=${FLAVOR_6}
      - SITE_KEY=${FLAVOR_6_KEY}
      - SITE_URL=${FLAVOR_6_URL}
      # - CLICKY_ANALYTICS_ID=${FLAVOR_6_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  # waterfront (gtvote) flavor:
  ${FLAVOR_7_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /${FLAVOR_7}
    environment:
      - FLAVOR=${FLAVOR_7}
      - SITE_KEY=${FLAVOR_7_KEY}
      - SITE_URL=${FLAVOR_7_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_7_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_7_PORT}:${FLAVOR_7_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_7_PORT}
      - FLAVOR=${FLAVOR_7}
      - SITE_KEY=${FLAVOR_7_KEY}
      - SITE_URL=${FLAVOR_7_URL}
      # - CLICKY_ANALYTICS_ID=${FLAVOR_7_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  # gt open spaces flavor:
  ${FLAVOR_8_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /${FLAVOR_8}
    env_file: ./.env-datasets
    environment:
      - FLAVOR=${FLAVOR_8}
      - SITE_KEY=${FLAVOR_8_KEY}
      - SITE_URL=${FLAVOR_8_URL}
    command: sh -c "git fetch && git checkout gtopenspace && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_8_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_8_PORT}:${FLAVOR_8_PORT}"
    env_file: ./.env-datasets
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_8_PORT}
      - FLAVOR=${FLAVOR_8}
      - SITE_KEY=${FLAVOR_8_KEY}
      - SITE_URL=${FLAVOR_8_URL}
      # - CLICKY_ANALYTICS_ID=${FLAVOR_8_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout gtopenspace && git pull --rebase && /platform/start.sh"

  # watershed flavor:
  ${FLAVOR_9_CONTAINER}_data:
    image: smartercleanup/platform-data:release-0.6.3
    volumes:
      - /${FLAVOR_9}
    environment:
      - FLAVOR=${FLAVOR_9}
      - SITE_KEY=${FLAVOR_9_KEY}
      - SITE_URL=${FLAVOR_9_URL}
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start-data-container.sh"

  ${FLAVOR_9_CONTAINER}:
    image: smartercleanup/platform:release-0.6.3
    ports:
      - "${FLAVOR_9_PORT}:${FLAVOR_9_PORT}"
    environment:
      - DEBUG=${DEBUG}
      - PORT=${FLAVOR_9_PORT}
      - FLAVOR=${FLAVOR_9}
      - SITE_KEY=${FLAVOR_9_KEY}
      - SITE_URL=${FLAVOR_9_URL}
      # - CLICKY_ANALYTICS_ID=${FLAVOR_9_CLICKY_ANALYTICS_ID}
      # - MAPBOX_TOKEN=${MAPBOX_TOKEN}
    restart: always
    command: sh -c "git fetch && git checkout master && git pull --rebase && /platform/start.sh"

  letsencrypt:
    image: lukeswart/letsencrypt
    entrypoint: ""
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - /var/lib/letsencrypt:/var/lib/letsencrypt
    ports:
      - "80"
      - "443"
    environment:
      - TERM=xterm
      - LETSENCRYPT_DOMAINS=${LETSENCRYPT_DOMAINS}
      - LETSENCRYPT_DEBUG_MODE=${LETSENCRYPT_DEBUG_MODE}
      - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}

  nginx:
    # image: nginx:stable-alpine
    image: lukeswart/nginx-letsencrypt
    links:
      - "${FLAVOR_1_CONTAINER}"
      - "${FLAVOR_2_CONTAINER}"
      - "${FLAVOR_3_CONTAINER}"
      - "${FLAVOR_4_CONTAINER}"
      - "${FLAVOR_5_CONTAINER}"
      - "${FLAVOR_6_CONTAINER}"
      - "${FLAVOR_7_CONTAINER}"
      - "${FLAVOR_8_CONTAINER}"
      - "${FLAVOR_9_CONTAINER}"
      - letsencrypt
    volumes_from:
      - "${FLAVOR_1_CONTAINER}_data"
      - "${FLAVOR_2_CONTAINER}_data"
      - "${FLAVOR_3_CONTAINER}_data"
      - "${FLAVOR_4_CONTAINER}_data"
      - "${FLAVOR_5_CONTAINER}_data"
      - "${FLAVOR_6_CONTAINER}_data"
      - "${FLAVOR_7_CONTAINER}_data"
      - "${FLAVOR_8_CONTAINER}_data"
      - "${FLAVOR_9_CONTAINER}_data"
      - letsencrypt
    volumes:
      - ./nginx-acme-challenge-envsubst.conf:/etc/nginx/nginx.conf
      - ./nginx-envsubst.conf:/etc/nginx/nginx-secure.conf
    environment:
      - MY_DOMAIN_NAME=${MY_DOMAIN_NAME}
    ports:
      - 80:80
      - 443:443
    restart: always

    # Our cron container that runs our letsencrypt container, and reloads our nginx container
  letsencrypt-nginx-cron:
    image: docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./letsencrypt-nginx-cron:/etc/cron.d/letsencrypt-nginx-cron
    # environment:
    #   - TZ=${TZ}
    # TODO: automate timezone script
    command: sh -c "apk add --update tzdata && cp /usr/share/zoneinfo/US/Pacific /etc/localtime && echo 'America/Los_Angeles' > /etc/timezone && date && apk del tzdata && chmod a+x /etc/cron.d/letsencrypt-nginx-cron && touch /var/log/crond.log && crontab /etc/cron.d/letsencrypt-nginx-cron && crond -l 0 -L /var/log/crond.log && echo 'starting nginx-cron' && tail -f /var/log/crond.log"
